<Window
    x:Class="Waveface.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Waveface.Client" 
     xmlns:fw="clr-namespace:Waveface.ClientFramework;assembly=Waveface.ClientFramework"
       xmlns:wvmodel="clr-namespace:Waveface.Model;assembly=Waveface.Model"
        Title="MainWindow" Height="498.5" Width="807" Loaded="Window_Loaded" KeyDown="Window_KeyDown">

    <Window.Resources>
        <DataTemplate DataType="{x:Type wvmodel:Service}">
            <Label Content="{Binding Name}" Foreground="White" Height="40" VerticalContentAlignment="Center"/>
        </DataTemplate>

        <!-- Photo Template -->
        <DataTemplate DataType="{x:Type wvmodel:ContentGroup}">
            <local:GroupContentItem Padding="10"/>
        </DataTemplate>

        <!-- Photo Template -->
        <DataTemplate DataType="{x:Type wvmodel:Content}">
            <local:ContentItem x:Name="content" RenderTransformOrigin="0.5,0.5" Padding="10" TagStatusChanged="content_TagStatusChanged">
                <local:ContentItem.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform/>
                        <RotateTransform/>
                        <TranslateTransform/>
                    </TransformGroup>
                </local:ContentItem.RenderTransform>
            </local:ContentItem>
        </DataTemplate>
        
        

        <!-- Main photo catalog view -->
        <Style TargetType="{x:Type ListBox}" x:Key="PhotoListBoxStyle">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBox}" >
                        <WrapPanel Margin="5" IsItemsHost="True" Orientation="Horizontal" 
                        ItemHeight="{Binding ElementName=ZoomSlider, Path='Value'}"  
                       ItemWidth="{Binding ElementName=ZoomSlider, Path='Value'}" 
                       VerticalAlignment="Top" HorizontalAlignment="Stretch" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Labeled photo catalog view -->
        <!--<Style TargetType="{x:Type ListBox}" x:Key="LabledPhotoListBoxStyle">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBox}" >
                        <WrapPanel Margin="5" IsItemsHost="True" Orientation="Horizontal" 
                        ItemHeight="120" 
                       ItemWidth="120"
                       VerticalAlignment="Top" HorizontalAlignment="Stretch" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>-->

        <!-- Style for an individual generic item -->
        <Style TargetType="{x:Type ListBoxItem}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}" >
                        <Border SnapsToDevicePixels="True" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="{TemplateBinding Background}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#445B6249"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid Background="#FF222222">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="302*"/>
            <!--<RowDefinition Height="25"/>-->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="150"/>
        </Grid.ColumnDefinitions>
        <Grid Background="#FF5D5D5D" Grid.Column="0" Grid.Row="0" Margin="0,0,0,1">
            <Grid.Effect>
                <DropShadowEffect ShadowDepth="1" BlurRadius="1" Direction="270"/>
            </Grid.Effect>
            <Label Content="Waveface" Foreground="#FFE0E0E0" VerticalAlignment="Center" HorizontalAlignment="Left" Grid.Row="0" FontSize="22" FontWeight="Bold" FontFamily="Aharoni">
                <Label.Effect>
                    <DropShadowEffect/>
                </Label.Effect>
            </Label>
        </Grid>
        <Grid Background="#FF5D5D5D" Grid.Column="1" Grid.Row="0" Margin="0,0,0,1">
            <Grid.Effect>
                <DropShadowEffect ShadowDepth="1" BlurRadius="1" Direction="270"/>
            </Grid.Effect>
            <Grid Background="#FF5D5D5D" Grid.Row="0">
				<DockPanel>
						 <Slider Name="ZoomSlider"
          Width="100"
          Orientation="Horizontal" 
          Minimum="200" 
          Maximum="400" 
          Value="240" 
          TickFrequency="80"
          TickPlacement="BottomRight" 
          SmallChange="5"
          LargeChange="20" DockPanel.Dock="Right" VerticalAlignment="Center" />
                <WrapPanel VerticalAlignment="Center"  DockPanel.Dock="Left">
                    <local:BackButton Content="BackButton" Height="20"   VerticalAlignment="Center" Width="20" Click="BackButton_Click" Margin="2"/>
                    <Label x:Name="lblContentLocation" Content="{Binding ContentPath}" Foreground="White"/>
                </WrapPanel>
			
		  </DockPanel>
            </Grid>
        </Grid>
        <Grid Background="#FF5D5D5D" Grid.Column="2" Grid.Row="0" Margin="0,0,0,1">
            <Grid.Effect>
                <DropShadowEffect ShadowDepth="1" BlurRadius="1" Direction="270"/>
            </Grid.Effect>
            <Label Content="Labeled photos" Foreground="#FFE0E0E0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
        <ListBox x:Name="lbxDeviceContainer" Grid.Column="0" Grid.Row="1" Background="#FF2E2E2E" BorderBrush="{x:Null}"
                  ItemsSource="{Binding IsAsync=True}"
                 SelectedIndex="0"
                 VirtualizingStackPanel.IsVirtualizing="True" 
                 VirtualizingStackPanel.VirtualizationMode="Recycling" SelectionChanged="lbxDeviceContainer_SelectionChanged" PreviewMouseLeftButtonDown="lbxDeviceContainer_PreviewMouseLeftButtonDown">
            <ListBox.Effect>
                <DropShadowEffect BlurRadius="1" ShadowDepth="1" Direction="0"/>
            </ListBox.Effect>
        </ListBox>

        <Grid Grid.Column="2" Grid.Row="1" Background="#FF2E2E2E">
            <Grid.Effect>
                <DropShadowEffect BlurRadius="1" ShadowDepth="1" Direction="180"/>
            </Grid.Effect>

            <Button Content="Play" HorizontalAlignment="Left" Height="31" Margin="10,100,0,0" VerticalAlignment="Top" Width="130"/>
            <Button Content="Share" HorizontalAlignment="Left" Height="31" Margin="10,136,0,0" VerticalAlignment="Top" Width="130"/>
            <Label x:Name="lblLabeledCount" HorizontalAlignment="Left" Height="32" Margin="10,12,0,0" Content="{Binding Count}" ContentStringFormat="{}Labeled {0} photos" VerticalAlignment="Top" Width="130" Foreground="White" HorizontalContentAlignment="Center"/>

            <Label HorizontalAlignment="Left" Height="25" Margin="10,70,0,0" Content="Action:" VerticalAlignment="Top" Width="130" Foreground="White" HorizontalContentAlignment="Left"/>


        </Grid>

        <Grid Grid.Column="1" Grid.Row="1">
            <ScrollViewer x:Name="svContentContainer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <ListBox x:Name="lbxContentContainer"
		Style="{StaticResource PhotoListBoxStyle}" 
                         ItemsSource="{Binding IsAsync=True}"
          IsSynchronizedWithCurrentItem="True"
          Margin="5"
          SelectedIndex="0"
		  MouseDoubleClick="OnPhotoClick"
             VirtualizingStackPanel.IsVirtualizing="True" 
                         VirtualizingStackPanel.VirtualizationMode="Recycling">
                    <ListBox.Resources>
                        <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsMouseOver,RelativeSource={RelativeSource Self}}" 
                         Value="True">
                                    <Setter Property="IsSelected" Value="True" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.Resources>
                </ListBox>
            </ScrollViewer>

        </Grid>
    </Grid>
</Window>
