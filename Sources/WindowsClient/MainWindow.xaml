<Window
    x:Class="Waveface.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Waveface.Client" 
     xmlns:fw="clr-namespace:Waveface.ClientFramework;assembly=Waveface.ClientFramework"
       xmlns:wvmodel="clr-namespace:Waveface.Model;assembly=Waveface.Model"
        Title="MainWindow" Height="498.5" Width="807" Loaded="Window_Loaded" KeyDown="Window_KeyDown">

    <Window.Resources>
        <!--<DataTemplate DataType="{x:Type wvmodel:Service}">
            <Label Content="{Binding Name}" Foreground="White" Height="40" VerticalContentAlignment="Center"/>
        </DataTemplate>-->


        <!-- Photo Template -->
        <DataTemplate DataType="{x:Type wvmodel:ContentGroup}">
            <local:GroupContentItem Padding="10"/>
        </DataTemplate>

        <!-- Photo Template -->
        <DataTemplate DataType="{x:Type wvmodel:Content}">
            <local:ContentItem x:Name="content" RenderTransformOrigin="0.5,0.5" Padding="10" TagStatusChanged="content_TagStatusChanged" PreviewMouseRightButtonDown="content_PreviewMouseRightButtonDown">
                <local:ContentItem.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform/>
                        <RotateTransform/>
                        <TranslateTransform/>
                    </TransformGroup>
                </local:ContentItem.RenderTransform>
            </local:ContentItem>
        </DataTemplate>
        
        

        <!-- Main photo catalog view -->
        <Style TargetType="{x:Type ListBox}" x:Key="PhotoListBoxStyle">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBox}" >
                        <WrapPanel Margin="5" IsItemsHost="True" Orientation="Horizontal" 
                        ItemHeight="{Binding ElementName=ZoomSlider, Path='Value'}"  
                       ItemWidth="{Binding ElementName=ZoomSlider, Path='Value'}" 
                       VerticalAlignment="Top" HorizontalAlignment="Stretch" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Labeled photo catalog view -->
        <!--<Style TargetType="{x:Type ListBox}" x:Key="LabledPhotoListBoxStyle">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBox}" >
                        <WrapPanel Margin="5" IsItemsHost="True" Orientation="Horizontal" 
                        ItemHeight="120" 
                       ItemWidth="120"
                       VerticalAlignment="Top" HorizontalAlignment="Stretch" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>-->

        <!-- Style for an individual generic item -->
        <Style TargetType="{x:Type ListBoxItem}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}" >
                        <Border SnapsToDevicePixels="True" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="{TemplateBinding Background}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#445B6249"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid Background="#FF3F3F3F">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="302*"/>
            <!--<RowDefinition Height="25"/>-->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="150"/>
        </Grid.ColumnDefinitions>
        <Grid Background="#FF7F7F7F" Grid.Column="0" Grid.Row="0" Margin="0,0,0,1">
            <Grid.Effect>
                <DropShadowEffect ShadowDepth="1" BlurRadius="1" Direction="270"/>
            </Grid.Effect>
            <Label Content="Favorite" Foreground="#FFD8D8D8" VerticalAlignment="Center" HorizontalAlignment="Left" Grid.Row="0" FontSize="22" FontWeight="Bold" FontFamily="Aharoni" FontStyle="Italic"/>
        </Grid>
        <Grid Background="#FF5D5D5D" Grid.Column="1" Grid.Row="0" Margin="0,0,0,1">
            <Grid.Effect>
                <DropShadowEffect ShadowDepth="1" BlurRadius="1" Direction="270"/>
            </Grid.Effect>
            <Grid Background="#FF7F7F7F" Grid.Row="0">
				<DockPanel>
						
                <WrapPanel VerticalAlignment="Center"  DockPanel.Dock="Left">
                    <local:BackButton Content="BackButton" Height="20"   VerticalAlignment="Center" Width="20" Click="BackButton_Click" Margin="2"/>
                    <Label x:Name="lblContentLocation" Content="{Binding Name}" Foreground="White"/>
                </WrapPanel>
			
		  </DockPanel>
            </Grid>
        </Grid>
        <Grid Background="#FF7F7F7F" Grid.Column="2" Grid.Row="0" Margin="0,0,0,1">
            <Grid.Effect>
                <DropShadowEffect ShadowDepth="1" BlurRadius="1" Direction="270"/>
            </Grid.Effect>
        </Grid>

        <Grid Grid.Column="0" Grid.Row="1" Background="#FF595959">
            <Grid.RowDefinitions>
                <RowDefinition Height="25"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Background="#FF595959">
                <Label Content="Sources" Foreground="#FFA5A5A5"/>
            </Grid>
            <TreeView x:Name="lbxDeviceContainer"  Grid.Row="1" Background="{x:Null}" BorderBrush="{x:Null}"
                  ItemsSource="{Binding IsAsync=True}"
                 VirtualizingStackPanel.IsVirtualizing="True" 
                 VirtualizingStackPanel.VirtualizationMode="Recycling">
            <TreeView.Resources>

                <Style
 TargetType="TreeViewItem">

                    <EventSetter
 Event="TreeViewItem.PreviewMouseLeftButtonDown" Handler="TreeViewItem_PreviewMouseLeftButtonDown"
 ></EventSetter>

                </Style>

            </TreeView.Resources>
            <TreeView.Effect>
                <DropShadowEffect BlurRadius="1" ShadowDepth="1" Direction="0"/>
            </TreeView.Effect>
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Path=Contents}">

                    <!-- Display the AnimalCategory by showing it's Category string -->
                        <Label FontWeight="Bold" Content="{Binding Path=Name}" Foreground="White"/>

                    <!-- Specify the nested template for the individual Animal items
             that are within the AnimalCategories.  E.g. “California Newt”, etc. -->
                    <HierarchicalDataTemplate.ItemTemplate>
                        <DataTemplate>
                            <Label Content="{Binding Path=Name}" Foreground="White" Height="30" VerticalContentAlignment="Center"/>
                        </DataTemplate>
                    </HierarchicalDataTemplate.ItemTemplate>

                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
        </Grid>

        <Grid Grid.Column="2" Grid.Row="1" Background="#FF595959">
            <Grid.Effect>
                <DropShadowEffect BlurRadius="1" ShadowDepth="1" Direction="180"/>
            </Grid.Effect>
            <Button Content="Share" HorizontalAlignment="Left" Height="31" Margin="10,159.333,0,0" VerticalAlignment="Top" Width="130" Background="#FF7F7F7F" BorderBrush="{x:Null}" Foreground="White"/>
            <Label x:Name="lblLabeledCount" HorizontalAlignment="Left" Height="32" Margin="10,12,0,0" Content="{Binding Count}" ContentStringFormat="{}Labeled {0} photos" VerticalAlignment="Top" Width="130" Foreground="#FFF2F2DA" HorizontalContentAlignment="Center"/>
            <Grid Margin="10,70.667,10,0">

            	<Label HorizontalAlignment="Left" Height="25" Margin="66,0,0,0" Content="On     Off" VerticalAlignment="Top" Width="64" Foreground="#FFA5A5A5" HorizontalContentAlignment="Left"/>
            	<Label HorizontalAlignment="Left" Height="25" Margin="0,26.667,0,0" Content="On TV" VerticalAlignment="Top" Width="64" Foreground="#FFC1F2DA" HorizontalContentAlignment="Left"/>
            	<Label HorizontalAlignment="Left" Height="25" Margin="0,51.667,0,0" Content="On Cloud" VerticalAlignment="Top" Width="64" Foreground="#FFC1F2DA" HorizontalContentAlignment="Left"/>
            	<Grid Margin="76.334,32.667,11,28.666">
            		<RadioButton Content="RadioButton" HorizontalAlignment="Left" Height="15.334" VerticalAlignment="Top" Width="12.666" RenderTransformOrigin="0.632,-0.174"/>
            		<RadioButton Content="RadioButton" HorizontalAlignment="Left" Height="15.334" Margin="30,0,0,0" VerticalAlignment="Top" Width="12.666" RenderTransformOrigin="0.632,-0.174"/>
         
            	</Grid>
            	<Grid Margin="76.334,57.667,11,3.666">
         
            		<RadioButton Content="RadioButton" HorizontalAlignment="Left" Height="15.334" VerticalAlignment="Top" Width="12.666" RenderTransformOrigin="0.632,-0.174"/>
            		<RadioButton Content="RadioButton" HorizontalAlignment="Left" Height="15.334" Margin="30,0,0,0" VerticalAlignment="Top" Width="12.666" RenderTransformOrigin="0.632,-0.174"/>


            	</Grid>


            </Grid>


        </Grid>

        <Grid Grid.Column="1" Grid.Row="1">
			<Grid>
				 <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <ScrollViewer x:Name="svContentContainer" Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <ListBox x:Name="lbxContentContainer"
		Style="{StaticResource PhotoListBoxStyle}" 
                         ItemsSource="{Binding IsAsync=True}"
          IsSynchronizedWithCurrentItem="True"
          Margin="5"
          SelectedIndex="0"
		  MouseDoubleClick="OnPhotoClick"
             VirtualizingStackPanel.IsVirtualizing="True" 
                         VirtualizingStackPanel.VirtualizationMode="Recycling">
                    <ListBox.Resources>
                        <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsMouseOver,RelativeSource={RelativeSource Self}}" 
                         Value="True">
                                    <Setter Property="IsSelected" Value="True" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.Resources>
                </ListBox>
            </ScrollViewer>
			<Grid Grid.Row="1" Background="#FF7F7F7F" Margin="1,0">
				 <Slider Name="ZoomSlider"
          Width="100"
          Orientation="Horizontal" 
          Minimum="200" 
          Maximum="400" 
          Value="200" 
          TickFrequency="50"
          TickPlacement="None" 
          SmallChange="5"
          LargeChange="20" VerticalAlignment="Center" HorizontalAlignment="Left" BorderThickness="10,0,0,0" />
				</Grid>
			</Grid>
        </Grid>
    </Grid>
</Window>
