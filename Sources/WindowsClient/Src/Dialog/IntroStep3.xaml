<Window x:Class="Waveface.Client.IntroStep3"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Waveface.Client"
        Width="600"
        Height="450"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None">


    <local:IntroLayout Margin="3" NavigatorImageUri="/Waveface.Client;component/Resource/3_number.png">
        <local:IntroLayout.Effect>
            <DropShadowEffect ShadowDepth="1" />
        </local:IntroLayout.Effect>
        <StackPanel Margin="0, 40">
            <TextBlock HorizontalAlignment="Center"
                       FontSize="20"
                       Text="{StaticResource intro_step3_title}"
                       TextWrapping="WrapWithOverflow" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Image Margin="20,20,0,0" Source="/Waveface.Client;component/Resource/3_card.png" />

                <Grid Grid.Column="1" Margin="20,60, 0, 0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Image Margin="3"
                           VerticalAlignment="Top"
                           Source="/Waveface.Client;component/Resource/3_icon1.png" />
                    <TextBlock Grid.Column="1"
                               FontSize="18"
                               Text="{StaticResource intro_step3_desc1}"
                               TextWrapping="WrapWithOverflow" />


                    <Image Grid.Row="1"
                           Margin="3"
                           VerticalAlignment="Top"
                           Source="/Waveface.Client;component/Resource/3_icon2.png" />
                    <TextBlock Grid.Row="1"
                               Grid.Column="1"
                               FontSize="18"
                               Text="{StaticResource intro_step3_desc2}"
                               TextWrapping="WrapWithOverflow" />
                </Grid>
            </Grid>
            <Button MinWidth="120"
                    Margin="0,-10,0,0"
                    HorizontalAlignment="Right"
                    Click="Button_Click"
                    Content="{StaticResource intro_step3_button}" />
        </StackPanel>
    </local:IntroLayout>


</Window>
